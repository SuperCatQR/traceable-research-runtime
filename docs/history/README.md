# Historical Records

This directory preserves completed plans, execution logs, superseded integration notes, and
naming audits as review evidence. These files are not current implementation contracts.

Current normative material lives in `../web-search-architecture.md`, `../adr/`, and `../database/`.
Only unfinished work belongs in `../plans/` and `../todo/`.
